{
  "name": "@cush/relative",
  "version": "0.0.1",
  "main": "lib/relative.js",
  "scripts": {
    "build": "coffee-build src -o lib",
    "prepublishOnly": "prepv -i || true"
  },
  "devDependencies": {
    "coffeescript": "^2.3.0",
    "wch-coffee": "^0.2.0"
  }
}
